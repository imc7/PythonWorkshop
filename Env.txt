git push --set-upstream https://github.com/imc7/PythonWorkshop.git master //Subir a git

pip install virtualenv // Instalar librería
virtualenv name_of_env // Creación del entorno virtual
.\name_of_env\Scripts\activate //Activar entorno virtual

pip install mediapipe
pip install opencv-contrib-python